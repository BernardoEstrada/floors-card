import type { FloorCardConfig } from "types";

export const stubConfig: Partial<FloorCardConfig> = {
  header: "Home Overview",
  show_floor_icons: "always",
  show_area_icons: "always",
  preferred_icons: { tamper: "mdi:alarm-light-outline" },
};